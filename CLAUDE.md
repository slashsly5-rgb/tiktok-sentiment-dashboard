# Project Instructions

## Deployment Rules

When deploying to Railway:

1. **Backend changes**: If the backend service is already deployed and any changes are made to the backend code, restart the backend service after deployment.

2. **Frontend-only changes**: If only the frontend service is changed or restarted, do NOT restart the backend service.
